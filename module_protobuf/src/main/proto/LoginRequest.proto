syntax = "proto3";
//生成的java类所在的包名
package com.v.module_protobuf;

//登录请求结构体
message LoginRequest {
  string username = 1;
  string pwd = 2;
}
//登录响应结构体
message LoginResponse {
  int32 code = 1;
  string msg = 2;
  string data = 3;
  map<string, string> action_log = 4;
}