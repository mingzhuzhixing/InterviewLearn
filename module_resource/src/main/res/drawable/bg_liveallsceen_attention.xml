<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <corners android:radius="@dimen/y20"/>

    <!--android:angle Integer，代表渐变颜色的角度，
     0 is left to right, 90 is bottom to top.
     必须是45的整数倍. 默认是 0.
    该属性只有在type=linear情况下起作用，默认的type为linear。-->
    <gradient
        android:angle="0"
        android:endColor="#FF00C9A3"
        android:startColor="#FF00BF7F"
        android:type="linear"/>
</shape>