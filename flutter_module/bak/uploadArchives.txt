//dada 临时add start
if(project.version.contains("-SNAPSHOT")){
   snapshotRepository(url: "http://172.17.0.172:8081/repository/maven-snapshots/") {
     authentication(userName: "admin", password: "moment")
   }
}else{
    repository(url: "http://172.17.0.172:8081/repository/maven-releases/") {
       authentication(userName: "admin", password: "moment")
    }
}
//dada 临时add end

